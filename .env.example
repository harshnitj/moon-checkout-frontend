# Backend API (orders, checkout config, payments, branding)
VITE_BACKEND_URL=https://moon-checkout-backend.vercel.app

# Local dev against local backend: comment out VITE_BACKEND_URL above and run
# moon-checkout-backend on port 3000 — Vite proxies /api to localhost:3000.
